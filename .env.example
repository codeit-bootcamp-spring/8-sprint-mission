# Docker Compose 배포용 예시 — 복사: cp .env.example .env

POSTGRES_DB=discodeit
POSTGRES_USER=discodeit_user
POSTGRES_PASSWORD=discodeit1234

# 운영에서는 반드시 강한 값으로 변경
JWT_SECRET=default-secret-key-that-is-at-least-32-bytes-long!123

# 선택
JVM_OPTS=-Xms256m -Xmx512m
