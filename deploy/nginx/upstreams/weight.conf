# Weight — server에 weight를 부여한 가중 라운드로빈 (다중 Task IP가 풀리면 peer별로 반영)
upstream backend_cluster {
    zone backend_cluster 64k;
    resolver 127.0.0.11 valid=10s ipv6=off;
    server backend:8080 weight=3 resolve;
}
